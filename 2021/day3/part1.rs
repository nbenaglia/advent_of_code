use std::fs;

fn main() {
  let input = fs::read_to_string("input.txt").expect("Unable to reafd file");
  
  let gamme_rate: i32;
  let epsilon_rate: i32;

  for line in input.lines() {
  }

  println!("Power consumption {:?}", decimal_gamme_rate * decimal_epsilon_rate);
}